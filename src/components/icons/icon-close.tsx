import { BaseIcon } from "./base-icon"

export function IconClose({ width, height }: { width?: number; height?: number }) {
	return <BaseIcon src="/icons/icon-close.svg" width={width} height={height} />
}
